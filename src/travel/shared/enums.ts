export enum FileType {
  logo = 'logo',
  banner = 'banner',
  extra = 'extra',
  main = 'main',
}

export enum FileDependentType {
  organizer = 'organizer',
  tour = 'tour',
}

export enum StatusEnum {
  INACTIVE = 1,
  ACTIVE = 2,
  DELETED = 3,
  BLOCKED = 4,
}
